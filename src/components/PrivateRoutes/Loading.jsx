import React from "react";
import loading from "./Loading.module.css";

const Loading = () => (
  <div className={loading.loader} style={{ marginTop: "20%", marginLeft: "50%" }}>
  </div>
);

export default Loading;